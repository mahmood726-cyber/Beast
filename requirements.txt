# Core runtime (meta-analysis engine, CLI, storage, reports) -- standard library
# plus numerical stack:
numpy>=1.24
scipy>=1.10

# Optional, per data source (install only what you use):
#   pyreadr>=0.5     # read Pairwise70 *.rda Cochrane files (the CSV path needs nothing)
#   requests>=2.28   # EuropePMC live source

# Development / tests:
#   pytest>=7.0
